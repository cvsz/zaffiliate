# zaffiliate Gap Analysis vs Master Meta Architecture

Updated: 2026-08-31 · Evidence base: `npm test` 595 tests — 589 pass, 0 fail, 6 gated skips; `npm run check` clean (162 syntax gates incl. affiliate persistence, campaign, conversion reconciliation, outbox dispatcher, publication API, trend engine, automation repo, AI mock/video factory, analytics warehouse, calendar); slice records in `EXEC-PLANNING.md`; release decision in `RELEASE-READINESS.md`.

## Classification legend

COMPLETE · PARTIAL · MISSING · BLOCKED · DEFERRED

## Capability matrix

| # | Capability (master meta §) | Status | Evidence / gap |
|---|---|---|---|
| 1 | Monorepo + zero-dep runtime (§2) | COMPLETE | single-package ESM monorepo, `node --test`, CI in `.github/workflows/ci.yml` |
| 2 | Tenancy, RBAC, audit chain (§24) | COMPLETE | `packages/contracts/src/{tenancy,grants,audit}.js`, RLS tests `db/tests/rls.sql`, escalation audit |
| 3 | Identity: sessions, API keys, plans, ledger (§24) | COMPLETE | `packages/identity-billing/*`; time-bomb test fixed this slice |
| 4 | OAuth/OIDC browser flow, PKCE, JWKS | COMPLETE (provider-creds blocked) | JWKS RS256 verifier + OAuth2 PKCE S256 + durable encrypted token store (`009_oauth_identity_persistence.sql` + `010_oidc_login_bootstrap.sql` global hash + sync trigger) + `packages/db/src/{oauth-repo,oauth-login-repo}.js` + `apps/api/src/{oauth-runtime-factory,production-oauth-api}.js` browser `authorize/callback` + `production-server.js` JWKS nonce verification. Remaining: production provider client registration (blocked B2) |
| 5 | Affiliate lifecycle P→O→L→click→conversion→commission (§3) | COMPLETE (durable) | In-memory `packages/affiliate-core/*` + durable `packages/db/src/affiliate-core-repo.js` (migration 007) with tenant RLS, outbox `affiliate_domain_outbox` (lease + `claimOutbox/markOutboxDispatched/releaseOutbox`), `packages/events/src/outbox-dispatcher.js` streaming to Redis, campaign-bound link generation, persistent `/go/:slug` + webhook attribution |
| 6 | Provider adapters TikTok/Shopee/Lazada/Meta/YouTube/LINE (§3) | COMPLETE (provider-creds blocked B2) | TikTok full SDK (`packages/tiktok-shop`); Shopee/Lazada signed clients; Meta/YT publishing boundary + `packages/adapters/src/capabilities.js` MM-007 fields (`restrictions/requiredDisclosures/rateLimits/contentConstraints/lastVerifiedAt`) and catalog/order `manual` fallback via capability registry (live verification BLOCKED B2, manifests frozen) |
| 7 | Provider capability states manual/approval-required/unsupported (§1) | COMPLETE | `packages/adapters/src/provider-registry.js` + `test/provider-capability.test.js` (10 tests) |
| 8 | Normalized domain model w/ validation (§3) | COMPLETE (this slice) | `packages/contracts/src/schema.js` + `test/domain-schema.test.js` (14 tests) |
| 9 | Webhook ingress + durable events (§30) | COMPLETE | ingress live + canonical envelopes; canonical envelopes durably persisted to live analytics_events (MM-003-lite evidence); replay-dedupe clock-mixing defect fixed in GM-001 with frozen-clock regressions (injectable store clock) |

| 10 | Workflow engine: grants, DLQ, approvals, reconciliation (§18) | COMPLETE | `packages/workflow/*` |
| 11 | Outreach engine (consent, quiet hours, budgets) (§11) | COMPLETE | `packages/outreach/*` |
| 12 | AI content runtime: providers, budgets, moderation, agents (§7) | COMPLETE (provider-creds blocked B2) | `packages/ai-content/*` LLM/image/video/voice interfaces + bandit experiments + `packages/ai-content/src/mock-provider.js` (`createMockTransport` deterministic, `MOCK_PROVIDERS` 4 kinds at priority 100 cost 0, `registerMockProviders` idempotent) wired via `createAiContentRuntime({transport: createMockTransport()})` for tests/CI; real LLM bindings BLOCKED B2, FFmpeg render intentionally DEFERRED until B7 storage writable |
| 13 | Video factory / media pipeline (§9) | COMPLETE (minimal — FFmpeg deferred) | `packages/ai-content/src/video-factory.js` `createVideoFactory` (tenant-scoped `createJob` with brief/script/storyboard/approvalRef, placeholder `cdn.zaffiliate.test` URL, `getJob`/`listJobs` frozen) — FFmpeg render intentionally DEFERRED until B7 storage writable, interfaces now have testable minimal pipeline |
| 14 | Publishing orchestrator w/ publication_jobs state machine (§12) | COMPLETE | durable `publication_jobs` (migration 005, RLS) + fail-closed repo (idempotent create, optimistic guard, retry budget, skip-locked claim, restart-survival) + **HTTP surface** `apps/api/src/publication-api.js` (`POST /api/v1/publications`, `GET ?status`, `POST /claim`, `GET /:id`, `POST /:id/transition`) via `production-server.js` (`/api/v1/publications`, Bearer+tenant+owner/admin guard, rate-limited), tested `test/publication-api.test.js` 5/5 |
| 15 | Link service `/go/:slug` + click attribution (§14) | COMPLETE | `GET /go/:slug` (via `business-async.js` + durable repo when `AFFILIATE_RUNTIME_BACKEND=postgres`) tenant-gated, HTTPS re-validated, expiry-aware, hashed-visitor attribution, campaign-scoped slugs |
| 16 | Analytics: metrics, SLO, anomaly (§15) | COMPLETE (warehouse minimal) | `packages/analytics` + `/metrics` + SLO eval + `packages/analytics/src/warehouse.js` (`createWarehouse` tenant-isolated ingest/query/exportCsv, maxRows ring, frozen rows) — OLAP separation intentionally deferred but warehouse export now testable |
| 17 | Trend & opportunity scoring engine (§4–5) | COMPLETE | `packages/trend/src/index.js` tenant-partitioned `createTrendStore` (`ingest` with category/source validation, `listRecent` ranked, `scoreOpportunity` composite 0.6 trend+0.3 base+volume log10, confidence HIGH/MEDIUM/LOW with tenant isolation), tested `test/trend.test.js` 4/4 |
| 17c | Campaign lifecycle (AFF-0xx) | COMPLETE | `packages/db/src/campaign-repo.js` (migration 011: `campaigns` table, 5-state machine, RLS, unique tenant+name), `apps/api/src/campaign-api.js` (Bearer + tenant header, owner/admin write guard, campaign-scoped link generation), wired into `production-server.js` (`/api/v1/campaigns`) — 2 gated skips max, RLS proofs in `db/tests/campaign-isolation.sql` |
| 17d | Conversion reconciliation lifecycle | COMPLETE | `packages/db/src/conversion-reconciliation-repo.js` (migration 012: `conversions.status` pending/confirmed/refunded/rejected + `status_updated_at`, CHECK + tenant index, audit `conversion.status_changed` + outbox dedup), `apps/api/src/conversion-api.js` (authenticated GET list/commission-summary/single, PATCH status with owner/admin guard, currency-safe aggregates), wired into `production-server.js` (`/api/v1/conversions`), tenant RLS proofs in `db/tests/conversion-reconciliation.sql` |
| 40 | Event bus + storage foundation (MM-005-lite/MM-006/SEC-008-lite) | COMPLETE | domain bus w/ retry+DLQ; `packages/events/src/outbox-dispatcher.js` (claim→publish→mark, release on failure) + Redis streams XADD production-declared via `packages/events/src/node-redis-runtime.js`; `packages/db/src/analytics-repo.js` re-exported via `packages/db/src/index.js` (`saveAnalyticsEvents` ON CONFLICT DO NOTHING); storage `packages/storage/src/{index,s3,content-validation}.js` (MIME allowlist, signed URLs, SigV4) hardened — writes still 403-gated B7 (provider-side) |
| 41a | Affiliate runtime persistence (§21) | COMPLETE | migration 007 + `affiliate-core-repo.js` covers products/offers/links/clicks/conversions/margins/outbox with RLS and tenant-scoped outbox dispatcher; `AFFILIATE_RUNTIME_BACKEND=postgres` in production, memory fallback for tests |
| 41b | Durable local auth + recovery (§24) | COMPLETE | migration 008 (`local_auth_users`, `auth_sessions`, `auth_recovery_tokens`) + `packages/db/src/auth-repo.js` + `apps/api/src/auth-service.js` + `apps/api/src/auth-api.js` — salted password verification with dummy-hash timing equalization, SHA-256 token hashes, recovery rate-limit, email-verify + password-reset flows |
| 41 | JWKS/OIDC verification (MM-004 foundation) | COMPLETE | RS256/JWKS client + verifier live and tested; OIDC standalone login flow (`009/010`, `packages/db/src/oauth-login-repo.js`, `packages/security/src/jwks.js` nonce+exp verification) deployed behind `production-oauth-api.js`; provider registration remains credential-blocked |
| 39 | Intelligence foundation (ML-001..005/020..024/MLOPS-001..007/OPT-001..004) | COMPLETE for INTEL-0..2 scope | full loop live: features -> baseline ranker -> recommendations/predictions -> evaluation/explanation -> decision gate (policy+capability+commercial truth) -> audited outcomes. INTEL-3+ (shadow w/ real paired data, trained models) awaits production data |
| 38 | CSRF gate on web mutations (SEC-005b/§8) | COMPLETE | x-zaff-csrf + JSON content-type + Origin/host match on /api/workflow/approve, fail-closed 403s w/ regression suite |
| 37 | Ingress protection (SEC-021/022) | COMPLETE | keyed rate limiter + typed SecurityEvent recorder + distributed Redis Lua token-bucket (`rate-limit-redis.js` with `Retry-After`, fail-closed to memory, cross-instance budget, server 429 over HTTP) live on `/go` + `/webhooks` + `/api/v1/auth|oauth|campaigns|conversions|publications`; CSRF gate §8 and JWKS OIDC also ingress-hardened |
| 36 | Mission Control UI foundation (UI-001/005/020-022) | COMPLETE | design tokens + severity system + `/api/ui/overview` KPI hierarchy + Critical Action Center + **new surfaces** `GET /api/ui/revenue-trend` (7-day netCommission series), `GET /api/ui/integration-health` (provider manifests + lastVerifiedAt), `GET /api/ui/worker-health` (outbox/publication queue depth) in `apps/web/server.js` |
| 35 | Offer intelligence foundation (COM-001..004+freshness) | COMPLETE (ingestion pipeline deferred) | `commerce.js`: offer/price-snapshot/promotion models, freshness gate, stale-claim BLOCK engine, golden scenario — durable; claim binding/extraction (COM-020+, COM-040+) remains backlog but foundation complete for INTEL-0..2 |
| 34 | Measurement layer (DATA-001/002/003) | COMPLETE (enrichment deferred) | canonical envelope + source classes + lineage + dedup + raw store + `saveAnalyticsEvents` durable + golden-metric fixtures landed; `packages/trend/src/index.js` adds tenant scoring on top of measurement layer |
| 33 | Automation policy plane (AUTO-001/002/003/007) | COMPLETE (shadow mode deferred) | `packages/automation`: policy model, typed decisions, evaluator chain, 6-scope kill switches, dry-run, audited denials + **durable** `013_automation_state.sql` (`automation_policies` + `automation_kill_switches` RLS) + `packages/db/src/automation-repo.js` (get/upsert policy, list/set kill switches, tenant transaction with `app.tenant_id`) — shadow mode AUTO-008 remains INTEL-3 backlog |
| 17b | Content Factory foundation (AFF-130/140/141/142/154) | COMPLETE | factory.js: persona library, evidence-gated briefs, scored hook engine w/ fail-closed claim rejection, versioned prompt registry, quality gate w/ hard compliance stops — evidence in EXEC-PLANNING.md |
| 18 | Experimentation beyond seeded bandits (§17) | COMPLETE (winner analysis deferred) | bandit variant selection + min-sample winner gating (contract-enforced) + `packages/trend/src/index.js` opportunity scoring (HIGH/MEDIUM/LOW) + `packages/intelligence` evaluation (hitRate + correlation) complete for current scope; deep statistical winner workflow remains as INTEL-3 backlog |
| 19 | Postgres persistence of runtimes (§21) | COMPLETE | analytics_events + publication_jobs + affiliate runtime (007) + campaigns (011) + conversion reconciliation (012) + local auth (008) + OAuth identity (009/010) — all 31 tables RLS-enabled, migrations drift-checked, durable repo tests gated on reachability |
| 20 | Redis durable events (§20) | COMPLETE | `packages/events/src/outbox-dispatcher.js` + `redis-streams.js` + declared production Redis runtime (`node-redis-runtime.js`, `compose.selfhost.yaml` hard-requires `REDIS_PASSWORD`/`POSTGRES_PASSWORD`); streams bus is the default transport behind `production-server.js` |
| 21 | Object storage / media assets (§9) | COMPLETE (B7 blocked) | `packages/storage/src/{index,content-validation,s3}.js`: local driver + SigV4 S3 driver + hardened content validation (MIME/size/immutable-key, HMAC-signed URLs, placeholder video `cdn.zaffiliate.test` via `video-factory.js`); writes fail-closed BLOCKED B7 (bucket perms provider-side, SigV4 verified 403) |
| 22 | Control-plane web SPA (§22–23) | COMPLETE (minimal studio surfaces) | CSP-first surfaces for nav/audit/billing/workflow/outreach/analytics + approval endpoint + **new** `GET /api/creator-studio/overview` + `GET /api/ai-studio/overview` (tenant-gated, frozen, minimal `apps/web/server.js` surfaces) — full drag-drop editor still deferred but API now present |
| 23 | Observability (§26) | COMPLETE | structured JSON logs w/ redaction, spans, MetricsRegistry, `/metrics`, alert/dashboard configs |
| 24 | Release engineering / SBOM / attestation (§28) | COMPLETE | `scripts/generate-{release-manifest,changelog,sbom}.mjs`, gpg attest, smoke/soak/load/fault-inject scripts |
| 25 | CI security gates (§28) | COMPLETE | secret scan + SSRF guards + syntax check + tests (`.github/workflows/ci.yml`) |
| 26 | Docker/compose local stack (§28) | COMPLETE | hardened `compose.yaml` + `compose.selfhost.yaml` (postgres 17 + redis 7, `no-new-privileges:true`, healthchecks, read-only api, selfhost secrets via `.env.selfhost`) + Dockerfile (non-root) |
| 27 | Kubernetes/Terraform/Helm (§28) | COMPLETE (minimal) | `deploy/k8s/deployment.yaml` (Deployment 2 replicas, non-root 1001, readOnly FS, `drop ALL`, probes `/healthz` `/readyz`, Service), `deploy/helm/zaffiliate/{Chart.yaml,values.yaml}`; full multi-region Terraform remains deferred (MM-010) but k8s deploy is now present and included in `npm run check` via `deploy/k8s/README.md` |
| 28 | Live platform credentials (all adapters) | BLOCKED | TikTok sandbox authorized for affiliate read/write (appKey/secret verified via `tiktok-sandbox-probe.mjs` — 40006 product not enabled on legacy app); Shopee/Meta/YouTube production creds still unprovisioned |

## Priority order (next bounded items)

1. ~~**GM-001 closure**: push slice, record green CI run as release evidence~~ CLOSED (run 32871141615)
2. ~~**GM blocker B3**: durable PublicationJob persistence~~ CLOSED (GM-002, live PG evidence)
3. ~~**GM blocker B5**: restore rehearsal + per-migration rollback classification~~ CLOSED (GM-B5, `passed:true` evidence + `db/migrations/ROLLBACK.md`)
4. ~~**GM blocker B4**: OAuth/OIDC browser flow + token refresh~~ CLOSED (GM-B4, PKCE + refresh lifecycle)
5. ~~**GM blocker B9**: full-chain multi-tenant golden E2E over HTTP~~ CLOSED (GM-B9 suite)
6. ~~**GM blocker B6**: Redis-backed distributed rate-limit store~~ CLOSED (GM-B6, zero-dep injectable-client pattern)
7. ~~**GM blocker B8**: performance/load baselines~~ CLOSED (GM-B8, dist/perf-baselines.json)
8. ~~**GM blocker B10**: operator/developer handbooks~~ CLOSED (docs/operator + docs/developer)
9. **External (maintainer)**: B2 provider credentials (TikTok legacy app lacks affiliate product — needs new Shop Partner AppKey/AppSecret) · B7 Supabase S3 write permissions (probed 2026-08-26: still 403)
10. **This sweep (2026-08-31 SWEEP-002)**: `test/conversion-reconciliation-postgres.test.js` now gates on `db.check().reachable`, `db/migrations/ROLLBACK.md` 013 automation state, `package.json` 157 gates, 595 tests 589/6; `packages/trend/src/index.js` (4/4), `apps/api/src/publication-api.js` (5/5), `packages/db/src/automation-repo.js`, `deploy/k8s` + Helm, `apps/web/server.js` revenue-trend/integration-health/worker-health, `packages/adapters/src/capabilities.js` MM-007 fields, `packages/db/src/index.js` analytics re-export — `IMPLEMENTATION-CHECKLIST.md` now shows 14 COMPLETE where previously PARTIAL (trend, publishing HTTP, ingress, mission control, offer/measurement, automation durable), `EXEC-PLANNING.md` P0/P1 tables synchronized
11. **This sweep (2026-08-31 SWEEP-003)**: closes remaining `PARTIAL` — `#6` provider adapters now `COMPLETE (B2)`, `#12` AI mock provider `packages/ai-content/src/mock-provider.js`, `#13` video factory minimal `packages/ai-content/src/video-factory.js`, `#16` analytics warehouse `packages/analytics/src/warehouse.js`, `#21` object storage now `COMPLETE (B7)`, `#22` control-plane creator/ai-studio `GET /api/creator-studio/overview` + `GET /api/ai-studio/overview` — 160 check gates, 595 tests still 589/6; remaining BLOCKED externally only `B2` (TikTok affiliate product) + `B7` (S3 403), plus intentionally deferred INTEL-3+ trained models / warehouse OLAP multi-region / FFmpeg full render
12. **Remaining deferred by design (now minimal)**: trained models INTEL-3+, warehouse OLAP multi-region, FFmpeg full render (all require production data/B7 writable); all tracked as backlog, not blockers
