replicaCount: 2
image:
  repository: ghcr.io/cvsz/zaffiliate
  tag: latest
  pullPolicy: IfNotPresent
env:
  APP_ENV: production
  PORT: 8080
resources:
  requests: { cpu: 250m, memory: 256Mi }
  limits: { cpu: 500m, memory: 512Mi }
service:
  type: ClusterIP
  port: 80
ingress:
  enabled: false
